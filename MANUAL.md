# yutori POS 操作マニュアル

機能別のリファレンスです。実際の作業順（オーダー入力 → 席連結 → 席移動 → 会計）で覚えたい場合は、スライド版の操作ガイドを参照してください。

---

## 目次

1. [ログイン](#1-ログイン)
2. [画面構成](#2-画面構成)
3. [フロアマップ](#3-フロアマップ)
4. [注文管理](#4-注文管理)
5. [ハッピーアワー](#5-ハッピーアワー)
6. [会計](#6-会計)
7. [日計・経費](#7-日計経費)
8. [商品マスタ](#8-商品マスタ)
9. [クーポン](#9-クーポン)
10. [レイアウト編集](#10-レイアウト編集)
11. [店舗設定](#11-店舗設定)
12. [Google Sheets 連携](#12-google-sheets-連携)
13. [付録：よくある操作](#付録よくある操作)

---

## 1. ログイン

### 1-1. 店舗選択

アプリを開くと店舗選択画面が表示されます。

1. 該当する店舗名のボタンをタップする
2. PIN入力画面に進む

> セッションは **毎日 AM 8:00** に失効します。8時をまたぐと再ログインが必要です。

### 1-2. PIN入力

- 数字キーパッドで **4〜6桁** の PIN を入力する
- `✓`（確認）ボタンをタップしてログイン
- PIN が間違っている場合はエラーメッセージが表示される
- `← 店舗選択に戻る` で店舗選択画面に戻れる

### 1-3. 初回ログイン（PIN未設定時）

PIN が未設定の店舗を選択すると「初期PINを設定」画面が表示されます。

1. 新しい PIN（4〜6桁）を入力する
2. 確認のためもう一度同じ PIN を入力する
3. 一致すると自動的にログインされる

---

## 2. 画面構成

ログイン後、ヘッダーに 3 つのタブが表示されます。

| タブ | 内容 |
|------|------|
| **フロア** | 席の配置・注文・会計。接客中はここだけで完結します |
| **レイアウト編集** | 席・パーツの配置変更 |
| **日計・管理** | 売上集計・経費・商品マスタ・クーポン・店舗設定 |

- データは Supabase に自動保存され、**他の端末にもリアルタイムで反映**されます。複数台の同時利用が可能です。
- 表示が古いと感じたら、ヘッダー右上の **更新** ボタンで最新状態を読み直せます。

---

## 3. フロアマップ

### 3-1. 席ステータスの色

フロアタブの上部に凡例が常時表示されます。

| 色 | ステータス | 意味 |
|----|-----------|------|
| グレー | 空席 | 使用されていない |
| アンバー | 予約 | 席の仮押さえ |
| レッド | 使用中 | 接客中（入店からの経過時間を表示） |
| ブルー | 会計済 | 会計完了。バッシング（片付け）待ち |
| 🔗 アイコン | 連結中 | 他の席と1伝票にまとまっている |

### 3-2. 席のタップ

席をタップするとサイドバーが開きます。

**空席の場合**
- サイドバーの「オーダー追加」で商品を追加すると、**使用中**に変わります
- 「予約にする」で席を仮押さえできます（もう一度押すと「予約を解除」）

**使用中の場合**
- 注文内容の確認、商品の追加・数量変更・削除、提供済みの記録
- ハッピーアワーの切り替え
- 「会計」の行をタップして会計画面へ

**会計済（青）の場合**
- **フロアマップで席をダブルタップ**すると、サイドバーを開かずに空席へ戻せます（最短）
- サイドバーの「バッシング完了」ボタンでも同じ操作ができます
- 空席に戻すと、顧客名・備考・ハッピーアワー・新規客の設定はリセットされます

### 3-3. 席の連結

複数の席を 1 伝票として管理できます。

1. フロアタブ上部の **「席を連結」** ボタンをタップ
2. まとめたい席を順にタップして選択（2席以上）
3. 上部の **「連結する」** ボタンで確定

- **最初にタップした席が親**になり、全席のオーダーがそこに集まります
- **人数は連結した席数**になります（3席の連結＝3名）。人数を入力する操作はありません
- 連結後はどの席をタップしても同じ伝票が開きます
- 会計・席移動はグループ全体が対象になります
- 予約席と使用中の席を混ぜて連結することはできません

**連結の解除**

サイドバーの「連結席」欄で、外したい席の `×` をタップします。オーダーが残っている場合は、どの商品を何点その席へ移すかを選ぶ画面が開きます。初期値は元々その席で注文された商品なので、そのまま確定すれば元どおりに分かれます。

> 解除すると人数も自動で減ります（3席の連結から1席を外せば2名になります）。

### 3-4. 席移動

1. フロアタブ上部の **「席移動」** ボタンをタップ
2. **移動元**の席をタップ（選べるのは使用中の席のみ。連結席はグループ全体が対象）
3. **移動先**の空席をタップ（連結席の場合は元と同じ席数ぶん選択。`2/3` のように進捗が出ます）
4. 上部の **「移動する」** ボタンで確定

- オーダー・人数・入店時刻・顧客名・ハッピーアワー・新規客の設定がそのまま移ります
- 移動元は自動的に空席へ戻ります
- 個別会計の途中でも移動でき、分割の進捗も引き継がれます

---

## 4. 注文管理

### 4-1. 席情報

サイドバー上部に表示・設定される項目です。

| 項目 | 操作 |
|------|------|
| 顧客名 | 席名の右の入力欄。会計履歴の見出しになります |
| 人数 | **入力不要**。`1席 = 1人` で自動計算されます（下記参照） |
| 新規 | `✦ 新規` ボタンで切り替え。日計の新規客数に反映されます |
| 入店時刻 | 最初のオーダーを入れた時刻が自動で記録されます |

**人数の数え方**

人数は **座っている席の数** から自動で決まります。入力する場所はありません。

- 単独の席 = **1名**
- 連結した席 = **連結した席数**（3席を連結すれば3名）

つまり、4名のお客様には4席ぶんを連結してください。人数は客単価・ハッピーアワーの計算（¥3,000/人、ドリンク¥600/人）に使われるため、**席の連結が実際のご案内人数と一致していることが重要**です。

### 4-2. 商品の追加

1. サイドバーの **「オーダー追加」** をタップ
2. 商品を探す（3通り）
   - **検索** — 上部の検索欄に入力。商品名・カテゴリ名への**部分一致**で、スペース区切りの複数語は全て含むものが表示されます（語順は不問）。全角英数・カタカナ／ひらがなの違いは無視されます
   - **カテゴリ** — 見出しをタップで開閉（検索中は自動で開きます）
   - **枠の色** — ドリンクは価格帯で枠線の色が変わります
3. 商品カードをタップすると数量が1ずつ増えます
4. 下部の **「◯点を注文に追加する」** で確定

**ドリンクの枠線色**

| 価格帯 | 色 |
|--------|----|
| 〜¥600 | 緑 |
| ¥700 | 青 |
| ¥800〜900 | 黄 |
| ¥1,000〜1,499 | 橙 |
| ¥1,500〜 | 赤 |

> 選択中の商品は緑の枠＋数量バッジで表示されます。確定前なら下部の一覧で `−` を押して数量を減らせます。

### 4-3. 数量変更・削除・提供済み

注文リストの各行で操作します。

- `−` / `＋` で数量を変更
- ゴミ箱アイコンで削除
- **「提供」** ボタンで提供済みを記録

### 4-4. オプションメモ

商品行の `💬 メモ追加` をタップして、「氷少なめ」などの指定を入力できます。会計履歴の内訳にも表示されます。

### 4-5. 備考

サイドバーの備考欄に、その組への申し送りを入力できます。

---

## 5. ハッピーアワー

### 5-1. 適用のしかた

サイドバーの **「注文内容」の右にあるボタン**で切り替えます（会計画面を開かなくても操作できます）。

- 未適用: アンバーの枠線ボタン `⚡ ハッピーアワー`
- 適用中: アンバー塗りつぶし `⚡ ハッピーアワー適用中 ¥3,000/人`

### 5-2. 料金の考え方

**1人あたり ¥3,000 の定額**に、以下が含まれます。

| 区分 | 会計への入り方 |
|------|---------------|
| HH基本料金 | ¥3,000 × 人数。**シーシャ・チャージ**はここに含まれます |
| ドリンク | 1人 ¥600 まで込み。**超えた分だけ**加算（4名なら¥2,400まで込み） |
| 別料金 | **アイスホース・トップ替え・ダークリーフ・シェア・アルコールボトル・ジュースボトル・ナイトチャージ** は、そのままの金額で加算されます |

- 適用中は、注文リストの対象商品に **HH** の印が付き、単品金額に取り消し線が入ります（基本料金に含まれている、という意味です）
- 会計の行にも **HH** の印が出るため、会計画面を開いた状態でも適用中か分かります
- `system` カテゴリに新しい商品を追加した場合、既定では **別料金** 扱いになります

### 5-3. ナイトチャージとの併用不可

ナイトチャージが注文に含まれているとハッピーアワーは適用できません。適用しようとすると「HH選択不可」の警告が表示されます。先にナイトチャージを削除してください。

---

## 6. 会計

### 6-1. 会計画面を開く

サイドバー下部の **「会計」**（合計金額が表示されている行）をタップすると、会計画面が全画面で開きます。もう一度タップで畳めます。

表示される内訳は上から順に、

1. HH適用中は「HH基本」「ドリンク超過」「その他」
2. 小計
3. 割引（クーポン適用時）
4. 消費税
5. 合計

### 6-2. クーポン適用

会計画面上部の選択欄から選びます。

| 割引タイプ | 内容 |
|-----------|------|
| 金額割引 | 指定金額を割引（例: 500円引き） |
| 率割引 | 合計に対する割合で割引（例: 10%引き） |
| 1品無料 | 一番高いドリンク1点が無料（**割引上限 ¥900**。それを超える商品は¥900まで） |

> クーポンを適用すると、合計金額は **100円未満切り捨て** になります（例: ¥3,690 → ¥3,600）。

### 6-3. 支払い方法

| ボタン | 内容 |
|--------|------|
| **現金** | 現金のみ |
| **クレペイ** | 次の画面で「カード決済（Square）」か「PayPay（店頭QR）」を選択 |
| **複合会計（現金＋クレペイ）** | 金額を分けて入力。両方の合計が請求額と一致すると確定できます |
| **個別会計** | お客様ごとに分けて支払う（6-4参照） |

- ボタンを押した時点で会計が記録され、席が **会計済（青）** になります
- Square連携が有効な場合、現金／カード決済は Square アプリへ切り替わって処理されます

### 6-4. 個別会計（分割・割り勘）

オーダーが2点以上あるときに使えます。

1. **「個別会計」** をタップ
2. 「何回に分けて支払いますか？」で回数を選ぶ
3. この回に含める商品を選ぶ（**数量単位**で選べます。同じ商品3点のうち1点だけ、も可能）
4. この回の支払い方法（現金／クレペイ／複合）を押して確定
5. 伝票は開いたまま「2回目 / 全3回」に進みます。**最後の回は残り全部**が対象になります

> ハッピーアワーの人数配分は自動です。合計が一括会計と一致するように配分され、端数は先の回が1名ぶん多く受け持ちます。

### 6-5. 会計取消

1. **日計・管理** タブ → **日計** → 会計履歴で対象を探す
2. **「取消」** ボタンをタップ（取消理由の入力はありません）
3. オーダーが未会計の状態で伝票に戻り、席も使用中に戻ります
4. そのまま会計をやり直せます

- 取消した会計は履歴に薄く「取消済」として残り、売上には含まれません

### 6-6. Square決済から戻れなかった場合

決済後にアプリへ戻る前に画面を閉じると、次回起動時に「未処理のSquare決済があります」と表示されます。Squareの決済履歴と自動照合した結果が表示されるので、**決済できていれば記録、できていなければ破棄**を選んでください。

---

## 7. 日計・経費

### 7-1. 集計サマリー

| 項目 | 内容 |
|------|------|
| 売上合計 | 税込合計金額 |
| 現金 | 現金支払い合計 |
| クレペイ | キャッシュレス合計 |
| 客数 / 組数 | 伝票単位の集計。**【　】内は新規客**の数 |
| 客単価 | 売上 ÷ 客数 |
| 経費 | その日に登録した経費 |
| 利益 | 売上 − 経費 |

> 営業日は**店舗設定の営業開始時刻**で区切られます。深夜の会計も前日ぶんとして集計されます。

### 7-2. 期間切り替え

| ボタン | 対象 |
|--------|------|
| **本日** | 当日の営業日のみ |
| **◯月** | 選択中の日付が属する月の合計 |
| **日付ピッカー** | 選んだ営業日1日だけ |

### 7-3. 経費入力

1. 日付を選択（レポートの表示期間と連動します）
2. レシート枚数を入力
3. 金額を入力
4. 引継ぎメモ（次のシフトへの申し送り）を入力
5. **「保存」** をタップ

> 「最終更新: HH:MM」で保存時刻が確認できます。

### 7-4. 会計履歴

期間内の会計一覧が表示されます。

- 各行には、支払い方法・**新規**・**⚡HH**・**分割 n/N**・**未同期**・**取消済** のバッジが付きます
- **行をタップするとオーダー内訳**（商品・数量・金額、小計・割引・税・合計）が開きます
- **赤枠**の行は未同期です。同期すると枠が消えます
- 取消済みの行はグレー表示になります

---

## 8. 商品マスタ

**日計・管理** タブ → **商品マスタ** で管理します。ここでの並び順が、そのままオーダー追加画面の表示順になります。

### 8-1. 並び替え

- **長押ししてスライド** — 商品・カテゴリのどちらも動かせます。他の項目の中央を越えた時点で入れ替わるので、指を離す前に結果が見えます
- **▲▼ ボタン** — 1つずつ動かします
- カテゴリを動かすと、**中の商品もまとめて**移動します
- 商品は同じカテゴリ内でのみ動きます。別カテゴリへ移す場合は鉛筆アイコンでカテゴリ名を編集してください

### 8-2. カテゴリの追加

右上の **「カテゴリ追加」** → カテゴリ名を入力 → `✓`

### 8-3. 商品の追加

1. カテゴリ見出しの **「商品追加」** をタップ
2. 商品名と価格を入力
3. `✓` で確定

### 8-4. 商品の編集・有効無効・削除

| アイコン | 操作 |
|---------|------|
| トグル | 有効／無効の切り替え（無効にするとオーダー画面に表示されません） |
| 鉛筆 | 商品名・価格・カテゴリの編集 |
| ゴミ箱 | 削除 |

> 季節メニューは削除せず**無効**にしておくと、また使えます。削除しても過去の売上データには影響しません。

---

## 9. クーポン

**日計・管理** タブ → **クーポン** で管理します。

### 9-1. クーポンの追加

1. **「クーポン追加」** をタップ
2. クーポン名・割引種別・割引値を入力

| 割引種別 | 内容 |
|---------|------|
| 金額割引 (円) | 指定金額を割引 |
| 率割引 (%) | 合計に対する割合で割引 |
| 1品無料（商品選択） | 一番高いドリンク1点が無料（上限 ¥900） |

### 9-2. 有効 / 無効の切り替え

トグルアイコンで切り替えます。無効にすると会計画面の選択肢に出なくなります。

---

## 10. レイアウト編集

**レイアウト編集** タブで席・パーツの配置を変更できます。

### 10-1. パーツの種類

| パーツ | 用途 |
|--------|------|
| チェア | 1人席 |
| ソファ | 複数人席 |
| カウンター | カウンター席 |
| 個室 | 個室ブース |
| 壁 | 仕切り |
| カウンター設備 | 機材・棚など |
| 通路 | 動線 |

### 10-2. 操作

- **配置** — パーツパネルからドラッグしてキャンバスへ
- **サイズ変更** — 選択してハンドルをドラッグ
- **回転** — 回転ボタンで 90° ずつ
- **削除** — 選択して削除ボタン
- **保存** — 「保存」ボタンで確定

> 保存しないと変更は破棄されます。

---

## 11. 店舗設定

**日計・管理** タブ → **店舗設定** で管理します。

### 11-1. 基本設定

| 項目 | 説明 |
|------|------|
| 店舗名 | アプリ内に表示される店舗名 |
| 営業開始時刻 | この時刻をまたぐまでを同一営業日として集計 |
| 消費税率（%） | 会計時の税率 |
| 会計済表示時間（秒） | 会計済（青）ステータスを表示する秒数 |

### 11-2. PINコードの変更

1. 「現在のPIN」に現在の PIN を入力
2. 「新しいPIN」に新しい PIN（4〜6桁）を入力
3. 「新しいPIN（確認）」に同じ PIN を再入力
4. **「変更を保存」** をタップ

> 現在の PIN が一致しない場合は保存できません。

### 11-3. ログアウト

「ログアウト」ボタンで店舗選択画面に戻ります。

---

## 12. Google Sheets 連携

会計データを Google スプレッドシートに同期します。

### 12-1. 自動同期

GAS のタイマートリガーにより定期的に同期されます。

### 12-2. 手動同期

日計タブの「スプレッドシート同期」カードで操作します。

- 未同期の件数がバッジで表示されます
- **「今すぐ同期」** で即時送信。完了すると「同期済」の表示に変わります

### 12-3. スプレッドシートの構成

| 構成 | 内容 |
|------|------|
| ファイル名 | `2026年 会計`（**年単位**で自動作成） |
| 行 | 1営業日 = 1行 |

**列**

`日付` / `曜日（祝日）` / `天気` / `来客数` / `組数` / `新規来客数` / `新規組数` / `売上` / `現金` / `キャッシュレス` / `割引` / `経費` / `経費枚数` / `利益`

> 天気は緯度経度から自動取得されます。GAS は店舗ごとに設置し、担当する店舗IDをスクリプトプロパティで指定します。

---

## 付録：よくある操作

### 席を間違えて案内した

フロアタブ上部の **「席移動」** で正しい席へ移します。オーダー・入店時刻・人数もそのまま移ります。

### 注文を間違えて追加した

注文リストの `−` で数量を減らすか、ゴミ箱で削除します。会計前ならいつでも修正できます。

### 会計後に金額が間違っていた

1. 日計タブ → 会計履歴 → **「取消」**
2. オーダーが伝票に戻るので、修正してから会計し直します

> 会計は何度でも取り消してやり直せます。無理にその場で計算し直さず、取消 → 修正 → 再会計の順で対応してください。

### 席が「使用中」から戻らない

未会計のオーダーが1件も無い席には **「空席に戻す」** ボタンが表示されます。これで空席に戻せます。

### 会計済みの席を早く空けたい

フロアマップで青い席を**ダブルタップ**します。

### リロード後にデータが消えた

すべてのデータは Supabase に保存されており、リロード後も自動で復元されます。表示が遅い場合はヘッダー右上の **更新** をタップしてください。

---

*本マニュアルは yutori POS システムの操作説明書です。*
